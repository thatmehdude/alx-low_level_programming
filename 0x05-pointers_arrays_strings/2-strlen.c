#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _strlen - gets the length of a string
 * @s: parameter
 * Return: 0
 */
int _strlen(char *s)
{
	int len;
	char w = *s;

	len = strlen(w);
	return (0);
}
