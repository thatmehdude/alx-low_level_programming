#include "main.h"
/** _abs - prints absolute value of an integer
 * Return: Integer
 */
int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
