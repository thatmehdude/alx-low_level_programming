#include "main.h"
/** _abs - prints absolute value of an integer
 * Return: Integer
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
